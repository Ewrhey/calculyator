#include "calculyator.h"

int main()
{
    string num1, num2, znac, test;
    getline(cin, num1);
    getline(cin, num2);
    getline(cin, znac);
    test = num1 + num2 + znac;
    while (only_numbers(test) == true){
        
    }
    return 0;
}
